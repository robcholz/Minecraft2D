//
// Created by robcholz on 7/1/23.
//
#include <cmath>
#include "gtest/gtest.h"
//#include "util/math/noise/PerlinNoise.hpp"


TEST(MathTest, BasicAssertion) {
	/*
	math::PerlinNoise noise;
	for (float i = 0; i < 300; i++) {
		double perlinValue = floor(20 * noise.noise(i, 0.8, 0.8));
		std::cout << "Perlin noise value at x = " << i << ": " << perlinValue << std::endl;
	}
	 */
}